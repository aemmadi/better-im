{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window. App-defined commands (invoke) are always allowed; this grants the core event listen/emit APIs used for live index updates.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
