// Phase 4 — Global unified timeline. Filled in by the frontend feature agent.
// Contract: `api.timelineFeed(before, limit)` -> TimelineItemDto[] (newest-first,
// keyset-paginated on the ISO `before` cursor).

export function TimelineView() {
  return (
    <div className="feature-view feature-placeholder">
      Unified timeline — coming in Phase 4.
    </div>
  );
}
